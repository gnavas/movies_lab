<h2 class="text-center">Actor Comments</h2>
 <div class="row">
        <div class ="col-md-10 col-md-offset-1">
          <table class ="table table-striped">
            <tr>
              <th>Actor</th>
              <th>Comment</th>
            </tr>
            <% @actor.comments.each do |comment|  %>
            <tr >
              <td ><%= comment.commentable.name%></td>
              <td ><%=comment.content %></td>
            </tr>
            <% end %>
          </table>     

<h2 class="text-center">Add Comment</h2>
     <%= form_for Comment.new, url: create_actor_comment_path("actor",@actor.id), method: :post do |form| %>
     <p>Comment:</p>
      <%= form.text_area :content, :rows => 5 %>
      <%= form.hidden_field :commentable_type, :value => "Actor" %>
      <%= form.hidden_field :commentable_id, :value => @actor.id %>
    <p><%= submit_tag "Add comment" %></p>
<% end %>